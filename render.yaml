services:
  - type: web
    name: qmeow-token-mcp
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python qmeow_mcp_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
